# Core ML / DL
python-dotenv
torch>=2.2
scikit-learn<1.5    # 1.5+ scipy/sklearn releases dropped numpy<2 support
scipy<1.13          # scipy 1.13+ requires numpy>=2.0
pandas
numpy<2              # torch 2.2.2 (the last macOS x86_64 build) predates numpy 2.x ABI support
matplotlib

# Fine-tuning
# transformers pinned <5.0: transformers 5.x requires torch>=2.4, which has
# no macOS x86_64 wheels (PyTorch dropped Intel Mac builds after 2.2.2)
transformers>=4.40,<5.0
peft
bitsandbytes
accelerate
datasets

# Retrieval / RAG
sentence-transformers
faiss-cpu

# Edge / quantization
# torch.ao.quantization ships with torch, no separate package needed

# Agentic / serving (optional, capstone extension)
fastapi
uvicorn

# Notebooks
jupyterlab
ipykernel
